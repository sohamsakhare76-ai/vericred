# --- Hardhat / deployment (root .env) ---
# Sepolia RPC endpoint (Alchemy, Infura, or similar)
SEPOLIA_RPC_URL=   

# Deployer private key (NEVER commit the real key; use a dedicated test wallet)
PRIVATE_KEY=

# Optional: for contract verification on Etherscan
ETHERSCAN_API_KEY=

# --- Shared / used to generate frontend/.env after deployment ---
PINATA_JWT=

# --- Frontend (frontend/.env) ---
VITE_CONTRACT_ADDRESS=
VITE_CHAIN_ID=11155111
VITE_PINATA_JWT=
VITE_PINATA_GATEWAY=
